{
  "name": "affectionalyzer",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "esbuild src/affect/sync.test.ts --bundle --platform=node --format=esm --outfile=node_modules/.cache/sync.test.mjs --log-level=error && node node_modules/.cache/sync.test.mjs"
  },
  "devDependencies": {
    "@types/node": "^20.14.4",
    "typescript": "^5.6.2",
    "vite": "^5.4.1"
  }
}
